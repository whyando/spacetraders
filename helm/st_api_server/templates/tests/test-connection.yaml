apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "st_api_server.fullname" . }}-test-connection"
  labels:
    {{- include "st_api_server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "st_api_server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
